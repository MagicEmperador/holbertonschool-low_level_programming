#include "main.h"
#include <stdio.h>
#include <stdlib.h>


/**
* main - adds numbers. checks if number
* @argc: number of arguments
* @argv: the vector that hold the arguments
*
* Return: returns 0 if number are not a digit return error
*/

